# ~/.bash_profile: executed by the command interpreter for login shells

# make login shells source normal config
source "$HOME/.bashrc"

# set shell programs
export PAGER=less
export MANPAGER=$PAGER
export EDITOR=vim
